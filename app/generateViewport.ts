export function generateViewport() {
  return {
    width: "device-width",
    initialScale: 1,
    minimumScale: 1,
    maximumScale: 1,
    userScalable: false,
    themeColor: "#f3df00",  // ta couleur thème globale ici
  };
}

